@model Application.ViewModels.CreateSerieViewModel

@{
    ViewData["Title"] = "Reproductor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    iframe {
        width: 100%;
        height: 35rem;
        border: 5px solid white;
        border-radius: 5px;
    }

</style>

<div class="container mt-5">
    <h1 class="text-center">ITLAStream Reproductor</h1>
    <div class="position-relative mt-5">
        <div class="shadow-sm overflow-hidden">
            <iframe src="@Model.VideoLink" title="ITLAStream Reproductor"  allow="accelerometer; autoplay; clipboard-write; encrypted-media; picture-in-picture;" allowfullscreen">
            </iframe>
        </div>
    </div>
        <div class="d-flex justify-content-end mt-4">
            <a class="btn btn-outline-warning" asp-controller="Home" asp-action="Index">Ir atras</a>
        </div>
</div>